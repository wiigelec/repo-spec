#!/usr/bin/env bash

set -u

script_path=${BASH_SOURCE[0]}
case $script_path in
  */*) script_dir=${script_path%/*} ;;
  *) script_dir=. ;;
esac

repo_root=$(cd -- "$script_dir/.." && pwd)

PYTHONDONTWRITEBYTECODE=1 exec python3 "$script_dir/github_field_policy.py" "$repo_root" "$@"
