#!/usr/bin/env bash

set -euo pipefail

root=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
mode=${1:---write}

"$root/repo/scripts/generate-docs" "$mode"
"$root/product/scripts/generate-docs" "$mode"
