#!/usr/bin/env bash
set -euo pipefail
root=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)
exec env PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$root/scripts${PYTHONPATH:+:$PYTHONPATH}" python3 "$root/scripts/initializer/cli.py" "$root" "$@"
