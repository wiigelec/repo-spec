#!/usr/bin/env bash

set -euo pipefail

root=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/.." && pwd)

"$root/validation/runners/root_validation.py" "$root"
"$root/repo/scripts/validate"
"$root/product/scripts/validate"
