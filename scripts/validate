#!/usr/bin/env bash

set -u

script_path=${BASH_SOURCE[0]}
case $script_path in
  */*) script_dir=${script_path%/*} ;;
  *) script_dir=. ;;
esac

repo_root=$(cd -- "$script_dir/.." && pwd)

exec python3 "$script_dir/validate_impl.py" "$repo_root" "$@"
