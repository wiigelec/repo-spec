# Root validation unit tests

No ordinary Python `unittest` modules are currently owned by the root validation domain. Existing root validation tests are framework/integration self-tests under `../self/`.
