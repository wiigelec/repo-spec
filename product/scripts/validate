#!/usr/bin/env bash

set -euo pipefail

root=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)
exec env PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$root/product/scripts${PYTHONPATH:+:$PYTHONPATH}" python3 "$root/product/scripts/validate_impl.py" "$root" "$@"
