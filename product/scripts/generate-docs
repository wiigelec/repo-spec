#!/usr/bin/env bash

set -euo pipefail

root=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)
mode=${1:---write}

exec env PYTHONDONTWRITEBYTECODE=1 python3 "$root/product/scripts/docgen.py" "$root" "$mode"
