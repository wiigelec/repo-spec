# Risks and unresolved decisions

Status: candidate; non-authorizing

## Risks

| Risk | Description | Impact | Controls and evidence | Trigger and owner |
| --- | --- | --- | --- | --- |
| R1: Existing-code drift | Implementation and tests predate or conflict with accepted product specifications. Passing tests encode superseded behavior and may pass when they should fail. | False conformance confidence; silent acceptance of non-conforming behavior in successor implementation. | B0 baseline must explicitly classify every composite key. No classification by file presence or all-tests-pass alone. Every `preserve` claim must cite specific observable accepted behavior and protecting test. | Owner: B0 governed issue. Closed when B0 matrix exists and no undifferentiated classification remains. |
| R2: Orchestration size | I5 composes 13 canonical stages, cross-increment identity propagation, preconditions, promotion gates, and four terminal outcomes in a single orchestrator. | Complexity may hide sequencing defects or missing failure-path coverage. | I5 E2E evidence must cover all four terminal outcome classes plus determinism and rejection. Focused component evidence for each stage exists in predecessor increments. No orchestrator may redefine product semantics. | Owner: I5 governed issue. Closed when all terminal boundaries have fault-injection E2E evidence. |
| R3: Filesystem transaction and promotion correctness | Atomic rename, post-rename commitment detection, indeterminate rename, cleanup failure, and same-filesystem isolation depend on OS/filesystem behavior not controlled by Python. | Silent data loss, partial promotion, or unrecoverable staging on rare filesystem states. | Fault-injection evidence at every finalization write and rename boundary. Pre-rename destination-absence recheck. Post-rename stat confirmation. No copy/fallback/retry. | Owner: I4 governed issue. Refresh when supported platform combinations change. |
| R4: Future-extension scope leakage | Implementation or tests may inadvertently support platform profiles, dry-run, resume, recovery, or remote retrieval, creating undeclared V1 capabilities. | Implementation may exceed accepted scope, making future-extension acceptance harder and creating hidden dependencies. | I1 must reject every unsupported profile and excluded behavior before mutation. E2E rejection evidence covers all six excluded future-extension domains. No V1 path accepts platform, remote, dry-run, resume, recovery, or migration input. | Owner: I1 and I5 governed issues. Refresh on each spec-acceptance cycle. |
| R5: Plan and correspondence drift after specification changes | A material acceptance, revision, or retirement affecting any accepted specification that this plan relies upon invalidates the corresponding plan sections. | Implementation proceeds under obsolete authority; correspondence claims no longer match accepted specs. | `repo.development-workflow` and `repo.implementation-plan` require material-specification impact review. This plan records the governing repair lineage and current accepted-spec authority used by each impact review rather than treating the original `d3cf252` planning basis as permanently authoritative. | Owner: any governing issue or PR that accepts a specification revision. Closed only after plan impact review. |
| R6: Duplicate bare requirement IDs | `INIT-FIN-001` through `INIT-FIN-008` appear in both `product.framework-installation` and `product.full-initialization`. | Bare-ID traceability is ambiguous; misassignment could invent or omit product semantics. | All traceability in this plan uses composite `<spec_id>::<requirement_id>` keys. B0, gates, and successor issues must preserve composite identity. | Owner: Patch 1, carried through all successor governed work. Closed when bare-ID ambiguity is eliminated through specification repair (separate governed work). |
| R7: Provenance/handoff semantic regression | Issues #255 and #257 repaired provenance ownership, pre-Git handoff timing, handoff disposition semantics, and classification-array ordering. Future changes could reintroduce producer/record mismatch, pre-promotion completion claims, overlapping classifications, or nondeterministic ordering. | Reopened authority conflict could invalidate I3-I5 plan sections and block implementation. | B0 and I3 evidence must validate current closed provenance fields, pre-Git handoff semantics, mutually disjoint classifications, presence/absence rules, and deterministic ordering against accepted specs. | Owner: any future specification-change issue affecting provenance/handoff. Reopen plan impact review on material change. |

## Unresolved decisions

| Decision | Description | Constraints from accepted specs | Status and closure condition |
| --- | --- | --- | --- |
| U1: Module evolution strategy | Whether to evolve existing modules incrementally (e.g., `staging.py`, `foundations.py`) or introduce new modules with compatibility shims. | Accepted specs define component behavior but do not prescribe file layout, class structure, or module boundaries. | Not resolved by this plan. Closure: governed implementation issue chooses or delegates. |
| U2: CLI diagnostic surfaces | Whether partial CLI commands (e.g., `init`, `stage`, `promote`, `git`) remain available as developer diagnostics or are removed/hidden to prevent lifecycle bypass. | `product.full-initialization::INIT-FIN-001` requires exactly one standard standard-V1 orchestrator entry point. It does not forbid auxiliary diagnostic commands. | Not resolved by this plan. Closure: governed implementation issue decides, consistent with `INIT-FIN-001`. |
| U3: Durability mechanism | How "durably written" is implemented for record files: `fsync`, atomic write-and-rename, directory sync, or combination. | Accepted specs require durable writes and mutually consistent dual-record pairs but do not prescribe the filesystem API sequence. | Not resolved by this plan. Closure: I4 governed implementation issue, with fault-injection evidence demonstrating durability under simulated failures. |
| U4: Indeterminate rename detection | How the product detects and reports an indeterminate promotion outcome when `os.rename` succeeds but post-rename stat cannot confirm the destination. | `product.transactional-staging::INIT-TST-001`, `product.transactional-staging::INIT-TST-003`, and `product.execution-report::INIT-RPT-004b` require an indeterminate outcome with caller diagnostic. Detection mechanism is not prescribed. | Not resolved by this plan. Closure: I4 governed implementation issue, with fault-injection evidence. |
| U5: Initializer identity, version, and timestamp injection | How `initializer_name`, `initializer_version`, and `initialization_timestamp` are supplied while maintaining declared equivalence for equivalent inputs. | `product.provenance-record::INIT-PRO-003` and `product.provenance-record::INIT-PRO-004` require these fields and ISO 8601 UTC timestamps. `product.content-equivalence::INIT-EQV-007` requires equivalent inputs produce equivalent provenance records except for accepted timestamp variability. | Partially constrained: timestamp must vary, version must be stable for a given release. Exact injection mechanism is not prescribed. Closure: I3 governed implementation issue. |
| U6: Evidence manifest format | The format and schema of the B0 classification matrix and the requirement-level evidence manifest are not prescribed. | Accepted specs do not define a machine-readable evidence-manifest artifact. | Not resolved by this plan. Closure: B0 governed issue, subject to `repo.validation` and plan documentation. |
| U7: Acceptance readiness evidence | How the candidate plan demonstrates compliance with `repo.implementation-plan` to support a governed acceptance decision. | `REPO-IPL-008` prohibits acceptance when controlling specs are missing, candidate, structurally invalid, contradictory, or absent from the manifest. Issues #255 and #257 repaired the known accepted-spec conflicts; issue #259 / PR #260 completed the final plan synchronization and post-patch clean-room review. | Post-#259 readiness synchronization complete. Closure: separately governed issue #261 plan-acceptance decision supported by current-spec validation, synchronized planning state, and planning-completeness evidence. |
