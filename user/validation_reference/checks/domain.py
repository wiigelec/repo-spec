"""Aggregate production validation for the owning domain."""

from __future__ import annotations


def validate_domain(context) -> None:
    """Run production validation checks registered by the owning domain."""
    return None
