"""Validation self-test runner template."""

from __future__ import annotations


def main(argv: list[str]) -> int:
    """Run validation-framework self-tests independently from ordinary unit tests."""
    raise NotImplementedError("materialized domain must bind its validation self-test runner")
