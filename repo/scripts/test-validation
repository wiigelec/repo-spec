#!/usr/bin/env bash

set -euo pipefail

root=$(CDPATH= cd -- "$(dirname -- "${BASH_SOURCE[0]}")/../.." && pwd)

exec env PYTHONDONTWRITEBYTECODE=1 PYTHONPATH="$root/repo/src:$root/repo${PYTHONPATH:+:$PYTHONPATH}" python3 "$root/repo/validation/runners/test_validation_impl.py" "$root" "$@"
